# Override to build from source instead of pulling prebuilt images.
# Usage: docker compose -f docker-compose.yml -f docker-compose.build.yml up --build
services:
  server:
    build:
      context: ./src
      dockerfile: server.dockerfile

  web:
    build:
      context: ./src
      dockerfile: web.dockerfile
